services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digital_heroes_lead_platform
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - JWT_SECRET=digital_heroes_super_secret_jwt_key_2026
    restart: always
